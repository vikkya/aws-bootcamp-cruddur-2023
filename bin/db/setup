#! /usr/bin/bash

-e # stop if any line fails
ABS_PATH=$(readlink -f "$0")
bin_path="$ABS_PATH/bin/db"

source "$bin_path/drop"
source "$bin_path/create"
source "$bin_path/schema-load"
source "$bin_path/seed"
# source "$bin_path/update_cognito_user_ids"